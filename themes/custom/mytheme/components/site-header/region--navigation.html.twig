{# {% include '@mytheme/site-header/site-header.twig' with {
    logo: site_logo,
    name: global_variables.site_name,
    menu_items: page.field_panel_inner_description[0],
  } only
%}

{{ kint(global_variables) }} #}